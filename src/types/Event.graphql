scalar JSON

type Event {
  title: String
  description: JSON
  start_date: String
  end_date: String
  images: [String]
  talks: [Talk]
}

type Query {
  findEvents: [Event]
}

type Mutation {
  submitEventComment(eventId: String): Comment
}

type Subscription {
  commentEventAdded: Comment
}
