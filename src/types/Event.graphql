type Event {
  title: String
  description: String
  start_date: String
  end_date: String
}

type Query {
  findEvents: [Event]
}

type Mutation {
  submitEventComment(eventId: String): Comment
}

type Subscription {
  commentEventAdded: Comment
}
